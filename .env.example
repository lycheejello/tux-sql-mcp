# Copy to .env and fill in values
SQL_SERVER=tuxton-acu-sql.database.windows.net
SQL_DATABASE=acu-inventory

# Auth: "az_cli" uses your local `az login` token (dev)
#       "managed_identity" uses Azure Managed Identity (prod/CI)
SQL_AUTH=az_cli
